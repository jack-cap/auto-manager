# Auto Manager Environment Configuration
# Validates: Requirements 10.1, 10.2, 10.3
#
# Copy this file to .env and update the values for your environment.
# For production, ensure all required values are set securely.

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Debug mode (set to false for production)
DEBUG=false

# Secret key for JWT token signing
# REQUIRED for production - generate with: openssl rand -hex 32
SECRET_KEY=change-me-in-production

# Encryption key for API key storage (Fernet key)
# REQUIRED for production - generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL connection URL (recommended for production)
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://automanager:automanager@localhost:5432/automanager

# For development, you can use SQLite instead:
# DATABASE_URL=sqlite+aiosqlite:///./automanager.db

# PostgreSQL password (used by docker-compose for the db service)
POSTGRES_PASSWORD=automanager

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis connection URL for caching
REDIS_URL=redis://localhost:6379/0

# Cache TTL in seconds (default: 5 minutes)
CACHE_TTL=300

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated)
# Update this for your production domain
CORS_ORIGINS=["http://localhost:3000"]

# =============================================================================
# JWT CONFIGURATION
# =============================================================================

# JWT algorithm (HS256 recommended)
JWT_ALGORITHM=HS256

# Access token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token expiration in days
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# LLM CONFIGURATION
# =============================================================================

# LMStudio server URL (for chandra_ocr and local models)
# Use host.docker.internal when running in Docker on macOS/Windows
LMSTUDIO_URL=http://host.docker.internal:1234/v1

# Ollama server URL (for local models)
# Use host.docker.internal when running in Docker on macOS/Windows
OLLAMA_URL=http://host.docker.internal:11434

# Default LLM provider (ollama, lmstudio, openai, anthropic)
DEFAULT_LLM_PROVIDER=ollama

# Default LLM model name
DEFAULT_LLM_MODEL=llama3

# OCR model name (chandra for chandra_ocr via LMStudio)
OCR_MODEL=chandra

# =============================================================================
# CLOUD LLM PROVIDERS (optional)
# =============================================================================

# OpenAI API key (if using OpenAI models)
OPENAI_API_KEY=

# Anthropic API key (if using Claude models)
ANTHROPIC_API_KEY=

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Backend API URL for the frontend
# Update this for your production deployment
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# =============================================================================
# MANAGER.IO CONFIGURATION
# =============================================================================

# Note: Manager.io API credentials are stored per-user in the database
# and encrypted with the ENCRYPTION_KEY above.
# Users configure their Manager.io connection through the application UI.
#
# Example Manager.io API URL format:
# http://your-manager-io-server:8080/api2

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Compose project name (optional)
COMPOSE_PROJECT_NAME=automanager
